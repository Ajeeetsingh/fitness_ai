# Minimal direct dependencies for running this API.
# Transitive dependencies (starlette, anyio, etc.) are resolved by pip.
fastapi>=0.121.1
uvicorn>=0.38.0
httpx>=0.28.1
pydantic>=2.12.4
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
requests>=2.32.5
beautifulsoup4>=4.12.3
jsonschema>=4.23.0

# Test runner (kept here to keep the repo "one file install" simple)
pytest>=8.3.4